export enum StorageKey {
  Theme = "theme",
  Font = "font",
}

export enum Theme {
  Light = "light",
  Dark = "dark",
}

export enum Font {
  Sans = "sans",
  Serif = "serif",
  Mono = "mono",
}

export enum HTTP_Method {
  GET,
}
